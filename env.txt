# Skopiuj do .env i uzupełnij wartości (prywatne dane poza repo)
DATABASE_URL=postgresql://dbuser:dbpass@db-host:5432/ostrans
JWT_SECRET=replace_with_strong_random_secret
PORT=3000

# CORS / frontend host (ustaw na produkcyjną domenę)
ALLOW_ORIGIN=https://ostrans.famisska.pl
MAIN_SITE=https://ostrans.famisska.pl

# Discord OAuth (ustaw na wartości z aplikacji Discord)
DISCORD_CLIENT_ID=your_production_client_id
DISCORD_CLIENT_SECRET=your_production_client_secret
# Callback musi wskazywać na Twój serwer (produkcja):
DISCORD_REDIRECT_URI=https://ostrans.famisska.pl/auth/discord/callback
